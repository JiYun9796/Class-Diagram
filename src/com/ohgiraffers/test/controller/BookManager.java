package com.ohgiraffers.test.controller;

import com.ohgiraffers.test.model.dto.BookDTO;

import java.util.ArrayList;

public class BookManager {
    /*
    * addBook(bookBookDTO) : void / 전달받은 북디티오를 생상자시 선언한 array리스트 객체에 추가
    * deleteBook(index:int)void / 전달받은 도서 번호로 array리스트 안의 정보 삭제
    * searchBook(bTitle: String) int  / 전달받은 제목의 도서 검색 후 출력
    * ptintBook(index:int)void / 전달받은 인덱스 내의 도서정보 출력
    * displayAll(): void / 도서목록 전체 출력
    * sortedBookList(select:int) : Array List < bookdto > 전달받은 번호를 기준으로 해당 카테고리 순으로 정렬해서 a<>에 담아 리턴.
    ptint booklist(br:arrayList < bookdo > void)
    *  ㄴ 전달받은 a<>를 향상된 for문을 이용하여 출력 */

    ArrayList<BookDTO> bookmanager = new ArrayList<BookDTO>();

    public void main(String[] args) {

//        bookmanager.add(int index, Book);

    }


}
